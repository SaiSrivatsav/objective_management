_schema-version: 3.3.0
ID: objective_management
description: A simple CAP project.
version: 1.0.0
modules:
- name: objective_management-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: objective_management-auth
  - name: objective_management-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: objective_management-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: objective_management-db
  parameters:
    buildpack: nodejs_buildpack
- name: objective_management-destination-content
  type: com.sap.application.content
  requires:
  - name: objective_management-destination-service
    parameters:
      content-target: true
  - name: objective_management_html_repo_host
    parameters:
      service-key:
        name: objective_management_html_repo_host-key
  - name: objective_management-auth
    parameters:
      service-key:
        name: objective_management-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: mboobjectives_objective_management_html_repo_host
          ServiceInstanceName: objective_management-html5-app-host-service
          ServiceKeyName: objective_management_html_repo_host-key
          sap.cloud.service: mboobjectives
        - Authentication: OAuth2UserTokenExchange
          Name: mboobjectives_objective_management_auth
          ServiceInstanceName: objective_management-auth
          ServiceKeyName: objective_management-auth-key
          sap.cloud.service: mboobjectives
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: objective_management-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: objective_management_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - mboobjectives.zip
      name: mboobjectives
      target-path: resources/
    - artifacts:
      - mboapproval.zip
      name: mboapproval
      target-path: resources/
- name: mboobjectives
  type: html5
  path: app/mboobjectives
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: mboapproval
  type: html5
  path: app/mboapproval
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: objective_management-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: objective_management-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: objective_management-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: objective_management-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: objective_management-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: objective_management_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: objective_management-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
